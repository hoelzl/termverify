- **De-duplicated the project description between `README.md` and
  `AGENTS.md`.** `README.md` is now the single canonical home of the public
  project description; the divergent one-sentence variant in `AGENTS.md` is
  replaced by a pointer, ending the drift between the two.
