- Fixed the ConPTY adapter's terminal receipt claiming `tier="os"` for geometries the pseudoconsole never adopted: `ConptyChild.spawn` now refuses requests that cannot survive the console's signed 16-bit `COORD` members (silent truncation to a smaller size, creation failure, or child death at console attach) and proves every other geometry by a probe child's read-back of the adopted size, cached per process — a request the console cannot adopt exactly fails the start with a structured geometry error naming the requested and adopted sizes instead of running the subject at a size the receipt never recorded ([#228](https://github.com/hoelzl/termverify/issues/228)).
